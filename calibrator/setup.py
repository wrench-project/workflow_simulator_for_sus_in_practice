from setuptools import setup, find_packages

setup(
    name="parsl_scheduling_simulator",
    version="0.1",
    packages=find_packages(),
    install_requires=["simcal"],  # Add dependencies if needed
    
)
